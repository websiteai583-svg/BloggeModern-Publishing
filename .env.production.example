# ==============================================================================
# BLOGGE PRODUCTION ENVIRONMENT CONFIGURATION (.env.production.example)
# Use these variables when deploying the backend to Google Cloud Run
# ==============================================================================

NODE_ENV=production
DATABASE_URL=
SESSION_SECRET=
PORT=3000

# Optional configuration:
ADMIN_EMAIL=websiteai583@gmail.com
ALLOWED_ORIGINS=https://localhost,capacitor://localhost
APP_URL=
GEMINI_API_KEY=
